#include <stdio.h>
#define SIZE 10
int ht[SIZE];
int hash(int key) {
    return key % SIZE;
}
void insert(int key) {
    int i = hash(key);
    while (ht[i] != -1)
        i = (i + 1) % SIZE;
    ht[i] = key;
}
int main() {
    for (int i = 0; i < SIZE; i++)
        ht[i] = -1;
    int n, key;
    scanf("%d", &n);
    while (n--) {
        scanf("%d", &key);
        insert(key);
    }
    for (int i = 0; i < SIZE; i++)
        printf("%d ", ht[i]);
}
