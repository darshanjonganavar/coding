#include <stdio.h>
int adj[10][10], visited[10], n;
void dfs(int s) {
    printf("%d ", s);
    visited[s] = 1;
    for (int i = 0; i < n; i++)
        if (adj[s][i] && !visited[i])
            dfs(i);
}
int main() {
    int s;
    scanf("%d", &n);
    for (int i = 0; i < n; i++)
        for (int j = 0; j < n; j++)
            scanf("%d", &adj[i][j]);
    scanf("%d", &s);
    dfs(s);
}
